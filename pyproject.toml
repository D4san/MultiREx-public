[build-system]
requires = [
    "setuptools",
    "numpy",
    "taurex>=3.1.14",
    "matplotlib",
    "tqdm",
    "pandas",
    "requests",
    "gdown",
    "pyarrow",
    "joblib",
    "astropy"
]
build-backend = "setuptools.build_meta"
